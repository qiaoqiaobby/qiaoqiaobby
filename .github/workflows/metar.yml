name: METAR Card
on:
  schedule:
    - cron: "17 * * * *"
  workflow_dispatch:
permissions:
  contents: write
concurrency:
  group: metar
  cancel-in-progress: false

jobs:
  render:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # 渲染失败即整个 job 失败、不执行 push，output 分支旧卡自然保留
      - name: Render METAR card
        run: python3 scripts/render_metar.py dist
      - name: Push to output branch
        run: |
          git clone --depth 1 --branch output "https://x-access-token:${{ github.token }}@github.com/${{ github.repository }}.git" out
          cp dist/*.svg out/
          cd out
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --quiet || { git commit -m "chore: update metar card $(date -u +%FT%TZ)"; git push || { git pull --rebase && git push; }; }
